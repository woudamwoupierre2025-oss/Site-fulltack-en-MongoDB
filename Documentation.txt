1. config
	pour la variable d'environnemnet

2. 	Comment crypter les mot de passe sur node.js 
	- utiliser la bibiotheque :  npm install save bcrypt

				userSchema.pre('save', async function(next) {
				const salt = await bcrypt.genSalt();
				this.password = await bcrypt.hash(this.password, salt);
					next();

			   });
