1. config
	pour la variable d'environnemnet

2. 	Comment crypter les mot de passe sur node.js 
	- utiliser la bibiotheque :  npm install save bcrypt

				userSchema.pre('save', async function(next) {
				const salt = await bcrypt.genSalt();
				this.password = await bcrypt.hash(this.password, salt);
					next();

			   });

//////////////// athentification

	1. 📌 Ça sert à quoi jsonwebtoken ?

jsonwebtoken est une bibliothèque Node.js qui permet de créer et vérifier des JWT (JSON Web Tokens).

👉 Les JWT servent à faire l’authentification sécurisée (login, session, API protégée).


	2. pour lire les cookies 

		npm install -save cookie-parser